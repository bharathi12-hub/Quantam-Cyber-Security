# QuantumShield backend dependencies
# Loose lower-bounds so pip selects wheels compatible with the local Python
# (including 3.14). Pin exactly in CI once a target interpreter is fixed.
fastapi>=0.115
uvicorn[standard]>=0.34
sqlalchemy>=2.0.36
pydantic>=2.10
pydantic-settings>=2.7
python-multipart>=0.0.20
cryptography>=44
reportlab>=4.2
